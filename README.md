# Face-Recognition-
This Python script uses the face recognition and open cv-python libraries to capture a user's face via a webcam, calculate its face encoding, and save that encoding to a file using pickle.
